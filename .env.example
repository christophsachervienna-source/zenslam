# Zenslam Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env with real values to source control

# Environment: dev, staging, or prod
ENVIRONMENT=dev

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# Stripe Configuration (get keys from https://dashboard.stripe.com/apikeys)
# Test mode keys start with pk_test_ and sk_test_
# Live mode keys start with pk_live_ and sk_live_
STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Stripe Apple Pay Merchant ID
STRIPE_MERCHANT_ID=merchant.com.zenslam.app

# Usage:
# Development:
#   flutter run --dart-define-from-file=.env
#
# Production build:
#   flutter build apk --dart-define-from-file=.env.prod
#   flutter build ios --dart-define-from-file=.env.prod
